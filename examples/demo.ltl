// examples/demo.ltl — runnable demo for ltl_toml_parser

import std::io
use ltl_toml_parser as lib

fn main() {
    io::println("ltl_toml_parser demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}